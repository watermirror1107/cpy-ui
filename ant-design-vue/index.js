import Vue from 'vue';
import {
  Row,
  Col,
  // Alert,
  // Slider,
  // Statistic,
  // Pagination,
  // ConfigProvider,
  // Layout,
  // Input,
  // InputNumber,
  // Button,
  // Radio,
  // Checkbox,
  // Select,
  // Card,
  // Form,
  // FormModel,
  // Modal,
  // Table,
  // Tabs,
  // Icon,
  // Badge,
  // Popover,
  // Dropdown,
  // Breadcrumb,
  // Spin,
  // Menu,
  // Drawer,
  // Tooltip,
  // Divider,
  // DatePicker,
  // TimePicker,
  // message,
  // Empty,
  // Result,
  // Steps,
  // Upload,
  // Switch
} from 'ant-design-vue';

// // 设置 table 的 size 默认值为 small
// Table.props.size.default = 'small';
//
// message.config({
//   maxCount: 1
// });

// Modal.props.okText.default = '确定';
// Modal.props.cancelText.default = '取消';

// Vue.use(Alert);
// Vue.use(Slider);
// Vue.use(Statistic);
// Vue.use(Pagination);
// Vue.use(Empty);
// Vue.use(ConfigProvider);
// Vue.use(Layout);
// Vue.use(Input);
// Vue.use(InputNumber);
// Vue.use(Button);
// Vue.use(Radio);
// Vue.use(Checkbox);
// Vue.use(Select);
// Vue.use(Card);
// Vue.use(Form);
// Vue.use(FormModel);
Vue.use(Row);
Vue.use(Col);
// Vue.use(Modal);
// Vue.use(Table);
// Vue.use(Tabs);
// Vue.use(Icon);
// Vue.use(Badge);
// Vue.use(Popover);
// Vue.use(Dropdown);
// Vue.use(Breadcrumb);
// Vue.use(Spin);
// Vue.use(Menu);
// Vue.use(Drawer);
// Vue.use(Tooltip);
// Vue.use(Divider);
// Vue.use(DatePicker);
// Vue.use(TimePicker);
// Vue.use(Result);
// Vue.use(Steps);
// Vue.use(Upload);
// Vue.use(Switch);
// Vue.prototype.$confirm = Modal.confirm;
// Vue.prototype.$message = message;
